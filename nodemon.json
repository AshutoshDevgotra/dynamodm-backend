{
  "watch": ["src"],
  "ext": "ts",
  "exec": "node --env-file-if-exists=/vercel/share/.env.project -r ts-node/register -r tsconfig-paths/register src/server.ts",
  "ignore": ["src/**/*.test.ts"],
  "env": {
    "NODE_ENV": "development"
  }
}
